def count_in_list(lst: list, item) -> int:
    """
    Count occurrences of item in list.

    Args:
        lst: The list to search in
        item: The item to count

    Returns:
        Number of times item appears in lst
    """
    return lst.count(item)
